{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/next",
        "config": {
          "installCommand": "pnpm install --no-frozen-lockfile && pnpm approve-builds bcrypt"
        }
      }
    ]
  }